{% block body %}
	<div class="min-h-screen flex flex-col items-center justify-center gap-10">

		<div class="text-center">

			<div
				class="inline-flex bg-white p-1 rounded-full border border-gray-200 shadow-sm">

				{# Bouton JOUR #}
				<button type="button" data-period="jour" class="js-period-btn text-gray-500 hover:text-gray-700 hover:bg-gray-100 text-sm font-medium py-1.5 px-6 rounded-full transition-all">
					Jour
				</button>

				{# Bouton SEMAINE (Actif par défaut) #}
				<button type="button" data-period="semaine" class="js-period-btn text-gray-500 text-sm font-medium py-1.5 px-6 rounded-full transition-all">
					Semaine
				</button>

				{# Bouton MOIS #}
				<button type="button" data-period="mois" class="js-period-btn text-gray-500 hover:text-gray-700 hover:bg-gray-100 text-sm font-medium py-1.5 px-6 rounded-full transition-all">
					Mois
				</button>

			</div>
		</div>

	</div>

	{{ encore_entry_script_tags('app') }}

{% endblock %}
