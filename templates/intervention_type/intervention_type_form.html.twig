{% extends 'base.html.twig' %}
{% block title %} Intervention Type : Fiche
{% endblock %}
{% block body %}

<section class="w-full mr-auto">
    <h1 class="text-2xl font-semibold text-gray-700 mb-6">Fiche Type d'intervention</h1>

    {{form_start(form)}}

    <div class="flex md:flex-row flex-col gap-3 mt-4">
        {% include "composants/form_champ_text.html.twig" with {
            'label': 'Nom du type d\'intervention - champ obligatoire',
            'field': form.name,
        } %}
    </div>

    <div class="flex flex-col gap-4 mt-4">
        {% include "composants/form_champ_text.html.twig" with {
            'label': 'Code du type d\'intervention - champ obligatoire',
            'field': form.color,
        } %}

    <div class="flex flex-col gap-4 mt-4">
        {% include "composants/form_champ_text.html.twig" with {
            'label': 'Description - champ obligatoire',
            'field': form.description,
        } %}
    </div>

    <div class="flex flex-col sm:flex-row gap-6 items-center pt-5">
        {% include 'composants/button.html.twig' with {
            label: 'Retour à la liste',
            type: 'cancel',
        } %}

    <div class="flex flex-col sm:flex-row gap-6 items-center pt-5">
        {% include 'composants/button.html.twig' with {
            label: 'Supprimer',
            type: 'danger',
        } %} 

        {% include 'composants/button.html.twig' with {
            label: 'Enregistrer les informations',
            type: 'confirm'
        } %}
    </div>
    {{ form_end(form) }}
</section>
{% endblock %}